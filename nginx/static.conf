server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA — all paths serve index.html (user configures headscale URL in the UI)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
